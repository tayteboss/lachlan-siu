import styled from 'styled-components';

const ProjectsModalWrapper = styled.section``;

const ProjectsModal = () => {
	return <ProjectsModalWrapper>ProjectsModal</ProjectsModalWrapper>;
};

export default ProjectsModal;
